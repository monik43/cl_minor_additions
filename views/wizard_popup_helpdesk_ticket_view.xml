<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="cl_wizard_popup_confirmation_helpdesk_ticket" model="ir.ui.view">
        <field name="name">cl.wizard.popup.confirmation.helpdesk.ticket</field>
        <field name="model">cl_minor_additions</field>
        <field name="arch" type="xml">
            <form string="Confirmar cambio de estado del ticket">
                <p class="oe_grey">
                    El estado al que estás cambiando el ticket tiene una plantilla de mail asociada a él y, por lo tanto, se enviará automáticamente, ¿Estás segur@ de que quieres cambiar de estado?
                </p>
                <footer>
                    <button name="action_confirm" string="Confirmar" type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>